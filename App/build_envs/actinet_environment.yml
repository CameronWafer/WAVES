name: WAVES_actinet
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - openjdk=11        # Java 11 is widely available on conda-forge; change to =8 only if actinet requires it
  - pip
  - pip:
      - actinet==0.7.0
